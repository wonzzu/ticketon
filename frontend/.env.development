# 백엔드 API 베이스 URL (절대 경로)
# 백엔드 SecurityConfig의 CORS allowedOrigins에 http://localhost:5173 등록되어 있어야 동작.
VITE_API_BASE_URL=http://localhost:8080

# 대기열 폴링 간격 (밀리초)
VITE_QUEUE_POLL_INTERVAL_MS=3000

# 좌석 선점 유지 시간 (초) — 7분
VITE_SEAT_HOLD_SECONDS=420
